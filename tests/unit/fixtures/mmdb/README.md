# MMDB test fixtures

Canonical MaxMind DB test databases, used by `tests/unit/mmdb_test.lua`
to validate `core/mmdb.lua` against files produced by MaxMind's own
`mmdbwriter` (the cross-library conformance corpus). Validating against
an independently-produced file avoids the "encoder and decoder share the
same bug" trap that a hand-rolled fixture would risk.

## Provenance

Vendored from `github.com/maxmind/MaxMind-DB`, directory `test-data/`
(commit `main`, fetched 2026-07-05). Downloaded verbatim, unmodified.

| File | ip_version | record_size | Purpose |
|---|---|---|---|
| `MaxMind-DB-test-decoder.mmdb` | 6 | 24 | Every data-section type (map, array, uint16/32/64/128, int32, string, bytes, double, float, boolean, pointer) |
| `MaxMind-DB-test-ipv4-28.mmdb` | 4 | 28 | 28-bit nibble-split node encoding + native-IPv4 start node |
| `MaxMind-DB-test-ipv6-32.mmdb` | 6 | 32 | 32-bit node encoding + IPv6 traversal + v4-in-v6 miss path |
| `GeoLite2-Country-Test.mmdb` | 6 | 28 | Realistic Country schema (`country.iso_code`, nested `names`) + v4-in-v6 hits |

Together these cover all three record sizes (24 / 28 / 32), both IP
families, the v4-in-v6 embedding, all data types, and the real Country
record shape the Phase D2 plugin reads.

## License

`github.com/maxmind/MaxMind-DB` is dual-licensed **Apache-2.0 OR MIT**
(see the repository's `LICENSE-APACHE` / `LICENSE-MIT`). Both are
GPL-3.0 compatible, so these fixtures may be redistributed inside this
GPL-3.0 project. The `GeoLite2-Country-Test.mmdb` contains synthetic
test data, not a real GeoLite2 distribution.

## SHA-256

```
6996ce679243c7f719b901ebe3b490048af2fb5965163f083857533841154fd8  GeoLite2-Country-Test.mmdb
1b34970ee2f8b05c03a478e90b8e5266a01ea2f9385f361bfd3d31edd8d4ce3a  MaxMind-DB-test-decoder.mmdb
fd0102ee5e1b72b17d00ee01599547e641186f6561f037b91f5df6102416dbb4  MaxMind-DB-test-ipv4-28.mmdb
fe96ba60cbdbecf88151735012407ae9b0638d54ff5a9658238f45aa595564ec  MaxMind-DB-test-ipv6-32.mmdb
```

Expected lookup values baked into the unit test were generated by the
reference `maxminddb` Python reader (v3.1.1), not by `core/mmdb.lua`.
